name: lerobot-policy
channels: [conda-forge, pytorch, nvidia]
dependencies:
  - python=3.12
  - pip
  # CUDA/PyTorch pins must be selected against the WSL driver after WSL is enabled.
  # LeRobot is installed from a recorded official commit with the async extra.
